class Config:
    """
    Configuration settings for the Flask application.

    Attributes:
        DEBUG (bool): Enables or disables debug mode.
        HOST (str): The host IP address the application will run on.
        PORT (int): The port number the application will listen to.
    """
    DEBUG = True
    HOST = "0.0.0.0"
    PORT = 5000
